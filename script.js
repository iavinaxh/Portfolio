document.addEventListener('DOMContentLoaded',()=>{const $=s=>document.querySelector(s),$$=s=>[...document.querySelectorAll(s)];const nav=$('#nav'),progress=$('#progress'),cursor=$('#cursor'),dot=$('#cursorDot');
window.addEventListener('scroll',()=>{const y=scrollY,h=document.documentElement.scrollHeight-innerHeight;progress.style.width=(h?y/h*100:0)+'%';nav.classList.toggle('scrolled',y>40)});
const reveal=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting){e.target.classList.add('visible');reveal.unobserve(e.target)}}),{threshold:.12});$$('.reveal').forEach(e=>reveal.observe(e));
let mx=innerWidth/2,my=innerHeight/2,cx=mx,cy=my;addEventListener('mousemove',e=>{mx=e.clientX;my=e.clientY;dot.style.left=mx+'px';dot.style.top=my+'px'});function cursorLoop(){cx+=(mx-cx)*.15;cy+=(my-cy)*.15;cursor.style.left=cx+'px';cursor.style.top=cy+'px';requestAnimationFrame(cursorLoop)}cursorLoop();$$('.magnetic').forEach(el=>{el.addEventListener('mousemove',e=>{const r=el.getBoundingClientRect();el.style.transform=`translate(${(e.clientX-(r.left+r.width/2))*.12}px,${(e.clientY-(r.top+r.height/2))*.12}px)`});el.addEventListener('mouseleave',()=>el.style.transform='')});
$$('[data-tilt]').forEach(card=>{card.addEventListener('mousemove',e=>{const r=card.getBoundingClientRect(),x=(e.clientX-r.left)/r.width-.5,y=(e.clientY-r.top)/r.height-.5;card.style.transform=`perspective(1000px) rotateX(${-y*4}deg) rotateY(${x*4}deg)`});card.addEventListener('mouseleave',()=>card.style.transform='')});
const canvas=$('#ambient'),ctx=canvas.getContext('2d');let pts=[],w,h;function resize(){w=canvas.width=innerWidth;h=canvas.height=innerHeight;pts=Array.from({length:Math.min(55,Math.max(18,Math.floor(w*h/28000)))},()=>({x:Math.random()*w,y:Math.random()*h,vx:(Math.random()-.5)*.25,vy:(Math.random()-.5)*.25,r:Math.random()*1.4+.4}))}function ambient(){ctx.clearRect(0,0,w,h);for(const p of pts){p.x+=p.vx;p.y+=p.vy;if(p.x<0||p.x>w)p.vx*=-1;if(p.y<0||p.y>h)p.vy*=-1;ctx.fillStyle='rgba(215,255,79,.35)';ctx.beginPath();ctx.arc(p.x,p.y,p.r,0,Math.PI*2);ctx.fill()}for(let i=0;i<pts.length;i++)for(let j=i+1;j<pts.length;j++){const a=pts[i],b=pts[j],dx=a.x-b.x,dy=a.y-b.y,d=Math.hypot(dx,dy);if(d<105){ctx.strokeStyle=`rgba(215,255,79,${.09*(1-d/105)})`;ctx.beginPath();ctx.moveTo(a.x,a.y);ctx.lineTo(b.x,b.y);ctx.stroke()}}requestAnimationFrame(ambient)}resize();addEventListener('resize',resize);ambient();
const form=$('#contact-form'),status=$('#form-status'),submit=$('#submit-label');if(form)form.addEventListener('submit',async e=>{e.preventDefault();status.textContent='Sending message…';submit.textContent='Sending…';const data=new FormData(form);try{const res=await fetch(form.action,{method:'POST',body:data,headers:{Accept:'application/json'}});const json=await res.json();if(res.ok&&json.success){status.textContent='Message sent successfully. I’ll get back to you soon.';form.reset()}else throw new Error(json.message||'Submission failed')}catch(err){status.textContent='Could not send right now. Please email avisingh21122003@gmail.com directly.'}finally{submit.textContent='Send message'}});
const prefersReduced=matchMedia('(prefers-reduced-motion: reduce)').matches;if(prefersReduced){document.documentElement.style.scrollBehavior='auto';$$('.reveal').forEach(e=>e.classList.add('visible'))}
});